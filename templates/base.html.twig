<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Bienvenue!{% endblock %}</title>
        <link rel="stylesheet" href="https://bootswatch.com/5/solar/bootstrap.min.css">

        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
                
          <div name="accueill" class="jumbotron text-center">
            <h1>Hotel Sogui</h1>
            <p>Resize this responsive page to see the effect!</p> 
          </div>
          <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <div class="container-fluid">
              <a class="navbar-brand" href= '{{path('accueil')}}' _msthash="1864226" _msttexthash="349570">Accueil</a>
              <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation" _msthidden="A" _msthiddenattr="1739517" _mstaria-label="320099">
                <span class="navbar-toggler-icon"></span>
              </button>
              <div class="collapse navbar-collapse" id="navbarColor01">
                <ul class="navbar-nav me-auto">
                  <li class="nav-item">
                    <a class="nav-link active" href="{{path('chambre_index')}}"><font _mstmutation="1" _msthash="622544" _msttexthash="114218">Chambres </font><span class="visually-hidden" _msthash="913744" _msttexthash="122200">(current)</span>
                    </a>
                  </li>
                  <li class="nav-item">
                    <a class="nav-link" href="{{path('reservation_new')}}" _msthash="622726" _msttexthash="329004">Reservation</a>
                  </li>
                </ul>

                {% if not app.user %}

                <form class="d-flex">
                  <ul class="navbar-nav me-auto">
                    <div>
                    <li class="nav-item">
                      <button href= "{{ path('app_login') }}"  class="btn btn-primary" _msthash="1333917" _msttexthash="116402">Connexion</button>
                    </li>
                    </div>
                    <div>
                    <li class="nav-item">
                      <button href= "{{ path('app_register') }}"  class="btn btn-primary" _msthash="1333917" _msttexthash="116402">Inscription</button>
                    </li>
                    </div>
                    </ul>
                </form>

                 {% else %}

                <form class="d-flex">
                  <button href= "{{ path('app_logout') }}"  class="btn btn-primary" _msthash="1333917" _msttexthash="116402">Deconnexion</button>
                </form>
                {% endif %}
              </div>
            </div>
          </nav>
<div class="container"> 
        {% block body %}{% endblock %}
</div>
</body>
</html>
